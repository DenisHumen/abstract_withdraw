web3>=7.6.0
eth-account>=0.13.0
httpx>=0.27.0
openpyxl>=3.1.2
pydantic>=2.7.0
pydantic-settings>=2.3.0
PyYAML>=6.0.1
python-dotenv>=1.0.1
rich>=13.7.0
typer>=0.12.0
tenacity>=8.3.0

playwright>=1.44        # браузерная ветка для AGW-кошельков (вход одним ключом через Privy)

# --- Опциональные (ставить при необходимости) ---
# zksync2>=1.2.0        # fallback-подпись EIP-712 type-113 на Abstract (если стандартная tx не пройдёт)
# После установки playwright выполнить: python -m playwright install chromium
